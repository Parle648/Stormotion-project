import React from 'react';

const GameModeCard = ({children}: {children: React.ReactNode}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default GameModeCard;